@namespace MarkdownConverter.WebApp.Components.Primitives
@using MarkdownConverter.WebApp.Core.ViewModels

<div class="tab @(IsActive ? "tab-active" : "") @(IsDragOver ? "tab-drag-over" : "")"
     draggable="true"
     @ondragstart="OnDragStart"
     @ondragover:preventDefault="true"
     @ondragover="OnDragOver"
     @ondragleave="OnDragLeave"
     @ondrop:preventDefault="true"
     @ondrop="OnDrop"
     @ondragend="OnDragEnd">
    @if (IsRenaming)
    {
        <AppTextInput CssClass="tab-rename-input"
                      Value="@RenameText"
                      ValueChanged="OnRenameTextChanged"
                      OnBlur="OnRenameBlur"
                      OnKeyDown="OnRenameKeyDown"
                      StopClickPropagation="true"
                      AriaLabel="Rename document"
                      AutoFocus="true" />
    }
    else
    {
        <button type="button"
                class="tab-select"
                role="tab"
                aria-selected="@IsActive"
                aria-label="@GetTabAriaLabel()"
                title="@Tab.FileName"
                @onclick="OnClick"
                @ondblclick:stopPropagation="true"
                @ondblclick="OnStartRename">
            <span class="tab-name">@TruncateName(Tab.FileName)</span>
            @if (Tab.IsDirty)
            {
                <span class="tab-dirty" aria-hidden="true">&bull;</span>
            }
        </button>

        <AppIconButton IconClass="tab-rename"
                       LucideIcon="pencil"
                       OnClick="OnStartRename"
                       Title="Rename tab" />
    }
    <AppIconButton IconClass="tab-close"
                   OnClick="OnClose"
                   Title="Close tab">&times;</AppIconButton>
</div>
